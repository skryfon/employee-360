# Employee360 backend configuration.
#
# Placeholder — this is Cycle 1 (project scaffolding, ticket A1). The
# real config schema and Viper wiring land in ticket A2 (config, DB
# connectivity & migration runner).
