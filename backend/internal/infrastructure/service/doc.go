// Package service contains implementations of the domain service ports
// declared in internal/domain/service — JWT issuance/validation
// (jwt_service.go), password hashing (bcrypt_service.go), and email
// delivery for OTP / magic links (mail_service.go).
//
// No service implementations are defined yet — this is Cycle 1 (project
// scaffolding). They are introduced alongside auth work starting in
// Cycle 2.
package service
