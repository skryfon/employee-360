// Package seeder bootstraps the system tenant, core roles, and the
// platform Super Admin, invoked from cmd/bootstrap.
//
// No seeding logic is defined yet — this is Cycle 1 (project
// scaffolding). It is introduced once roles/users exist, starting in
// Cycle 2.
package seeder
