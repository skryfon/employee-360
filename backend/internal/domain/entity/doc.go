// Package entity contains the core domain models for Employee360
// (Tenant, User, Role, UserRole, Department, Position, Holiday,
// HolidayCategory, AuditLog).
//
// This package is part of the domain layer: it must never import Gin,
// GORM, database drivers, or any other external framework.
//
// No entities are defined yet — this is Cycle 1 (project scaffolding).
// Entities are introduced starting in Cycle 2.
package entity
